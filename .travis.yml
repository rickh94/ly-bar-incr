language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "pypy3"

before_install:
  - pip install pytest-cov
  - pip install codecov

install: pip install .
script: pytest --cov=ly_bar_incr

after_success:
  - codecov
